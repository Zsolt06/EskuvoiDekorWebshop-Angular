import { FormatPricePipePipe } from './format-price.pipe';

describe('FormatPricePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPricePipePipe();
    expect(pipe).toBeTruthy();
  });
});
